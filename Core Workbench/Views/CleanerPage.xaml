<UserControl x:Class="Core_Workbench.Views.CleanerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgBrush}">
    <Grid Margin="32,26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Temp Cleaner" Style="{StaticResource HeaderText}" />
            <TextBlock Text="Reclaim disk space from Windows scratch folders" Style="{StaticResource SubtleText}"
                       Margin="0,2,0,0" />
        </StackPanel>

        <!-- Summary + actions -->
        <Border Grid.Row="1" Margin="0,18,0,0" CornerRadius="12"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="{StaticResource CardColor}" Offset="0" />
                    <GradientStop Color="{StaticResource SurfaceAltColor}" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid Margin="20,16">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Sparkle mark -->
                    <Border Width="46" Height="46" CornerRadius="12" Background="{StaticResource AccentGradient}"
                            VerticalAlignment="Center">
                        <Path Width="22" Height="22" Stretch="Uniform" Stroke="White" StrokeThickness="1.6"
                              StrokeLineJoin="Round" HorizontalAlignment="Center" VerticalAlignment="Center"
                              Data="M12 3 L13.2 10.8 L21 12 L13.2 13.2 L12 21 L10.8 13.2 L3 12 L10.8 10.8 Z" />
                    </Border>
                    <StackPanel Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="RECLAIMABLE SPACE" Style="{StaticResource SectionLabel}" />
                        <TextBlock x:Name="TotalText" Text="—" FontSize="32" FontWeight="Bold"
                                   Margin="0,1,0,0" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="ScanButton" Content="Rescan" Style="{StaticResource GhostButton}"
                            Click="Scan_Click" Margin="0,0,10,0" />
                    <Button x:Name="CleanButton" Content="Clean selected" Style="{StaticResource PrimaryButton}"
                            Click="Clean_Click" IsEnabled="False" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Target list -->
        <ScrollViewer Grid.Row="2" Margin="0,16,0,0" VerticalScrollBarVisibility="Auto" Padding="0,0,6,0">
            <ItemsControl x:Name="TargetList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="row" Background="{StaticResource CardBrush}" CornerRadius="12"
                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                Padding="16,14" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <CheckBox Grid.Column="0" IsChecked="{Binding Selected}"
                                          VerticalAlignment="Center" Margin="0,0,14,0" />

                                <!-- Icon tile -->
                                <Border Grid.Column="1" Width="42" Height="42" CornerRadius="11"
                                        Background="{StaticResource SurfaceAltBrush}"
                                        VerticalAlignment="Center" Margin="0,0,14,0">
                                    <Path Width="22" Height="22" Stretch="Uniform"
                                          Data="{Binding Icon}" Stroke="{StaticResource Accent2Brush}"
                                          StrokeThickness="1.6" StrokeLineJoin="Round"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" />
                                    <TextBlock Text="{Binding Description}" Style="{StaticResource SubtleText}"
                                               TextTrimming="CharacterEllipsis" Margin="0,1,0,0" />
                                    <TextBlock Text="{Binding Status}" FontSize="11"
                                               Foreground="{StaticResource TextMutedBrush}" Margin="0,3,0,0" />
                                </StackPanel>

                                <StackPanel Grid.Column="3" VerticalAlignment="Center"
                                            HorizontalAlignment="Right" Margin="12,0,0,0">
                                    <TextBlock Text="{Binding SizeText}" FontWeight="Bold"
                                               FontSize="17" HorizontalAlignment="Right" />
                                    <TextBlock Text="{Binding DetailText}" Style="{StaticResource SubtleText}"
                                               FontSize="11" HorizontalAlignment="Right" Margin="0,1,0,0" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding Selected}" Value="True">
                                <Setter TargetName="row" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <Grid Grid.Row="3" Margin="2,14,2,0">
            <TextBlock x:Name="FooterStatus" Style="{StaticResource SubtleText}" VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Select:" Style="{StaticResource SubtleText}" VerticalAlignment="Center" />
                <Button Content="All" Style="{StaticResource LinkButton}" Click="SelectAll_Click" Margin="8,0,0,0" />
                <TextBlock Text="·" Style="{StaticResource SubtleText}" VerticalAlignment="Center" Margin="6,0" />
                <Button Content="None" Style="{StaticResource LinkButton}" Click="SelectNone_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
